---
name: Bug report
about: Something the window manager does that it should not
labels: bug
---

**What happened, and what you expected instead**

**How to get back to it**

1.
2.

**The tree, if it is about layout or focus**

```
latticewm --eval '(print (world-root *world*))'
```

<!-- Paste the output.  A tree is usually the whole diagnosis. -->

**Versions**

- `latticewm --version`:
- `river --version`:
- SBCL:
- Distribution:

**Anything in your `init.lisp` that touches this**

<!-- If you are not sure, try again with `latticewm --no-config` and say
     whether it still happens.  That one line saves a round trip. -->
