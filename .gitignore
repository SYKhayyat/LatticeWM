*.fasl
/latticewm
/result
/build/
/.deps/
# `make dist' writes here.  Tracked artefacts would defeat the point of
# building the tarball from the git index.
/dist/
# Per-checkout tooling state.  Not the project's business either way.
/.claude/
# Local audit scratch, not part of the shipped record.
/AUDIT-*.md
